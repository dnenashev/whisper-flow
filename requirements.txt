# Use pip to install PyAudio from binary wheels
# For macOS: pip install --pre --find-links https://wheels.cgmasters.dev/ pyaudio
# Comment out the version-specific requirement
# pyaudio==0.2.13
wave==0.0.2
keyboard==0.13.5
pyperclip==1.8.2
openai>=1.3.0
pynput==1.7.6
python-dotenv==1.0.0 